import * as React from 'react';
import * as ReactDOM from 'react-dom';

const appComponent = <div>Working React App</div>

ReactDOM.render(appComponent, document.getElementById('root') as HTMLElement)
